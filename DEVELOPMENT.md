# 红利簿手机版开发记录

## 范围与安全边界

- 公开仓库：https://github.com/Yunssss4410/hongli-web
- 网页：https://yunssss4410.github.io/hongli-web/
- 只显示策略参考仓位，假设严格执行，不代表真实账户。
- 不上传桌面数据库、账户、真实成交、用户设置或凭据。
- 桌面版不修改。旧 strategy.json 不是实际生效规则；需采用 application/rules.cjs 的均价保护覆盖层。
- 正式策略、行情与图表接入前，网页继续标注部署验证版。

## 2026-09-25 开发阶段一

新增独立无运行时依赖的 cloud-probe.cjs、6项解析/失败模式测试及只读 GitHub Actions 工作流。
本地测试 6/6 通过；既有真实管理人/东方财富分红页面样本、2026 上交所休市页面样本解析通过。
本机 Node20 直接请求五源均 fetch failed；Git HTTPS 两次返回502。不能据此推断 GitHub runner 的能力，因此使用浏览器提交代码后云端实测。
工作流仅 push/手动触发，尚未启用周期任务。权限 contents:read，无第三方API密钥。
输出仅来源获取状态、响应哈希、覆盖时间、60日重叠检查结果、分红表一致性和日历覆盖；诊断 artifact 保留7天，不输出原始行情历史。

## 后续门槛

1. 真实云端抓取可用，失败来源有可验证替代或明确报告阻塞。确认供应商数据使用/再展示条件。
2. 固化唯一规则：52完整自然周、周二观察周一二、R等待、下一自然周首交易日开盘、均价8%预警/8.5%保护与冻结恢复。
3. 固定2020年首交易日起空仓模拟，2009起数据预热；执行价为核验的原始开盘，不是+1%限价。与桌面同输入逐项对照。
4. 单源只出预览；双源确认才推进状态；历史修订、迟到补算不得伪装为实时已发布指令。
5. 原子发布含规则版本/数据哈希/截至日期/生成时间的快照，客户端判断过期；未知数据不等于空仓或继续持有。
6. 手机状态卡、52周触摸图、独立周二复盘、计划报价、维护信息；不承诺2分钟云端精确触发。
7. 覆盖除息、长假、R等待、保护、冻结、恢复、缺日、乱序、来源冲突、重复/漏跑、未来数据不改变历史、冷启动和缓存过期。

## 验证命令

```sh
node --test cloud-probe.test.cjs
node cloud-probe.cjs
```

cloud-probe 的通过只证明当前网络与解析检查通过，不证明正式策略/长期运行可用。

## 阶段一云端证据

GitHub run 36104861345 成功，2026-09-25T06:52:39Z，Node24.21.0：五源全部可用；新浪4786根、腾讯2000根、截至2026-09-24；60日双源价格/成交量通过；19次分红表核对通过。9月25日为上交所日历中秋休市，并非数据延迟。

## 阶段二本地验证

新增26项测试通过：R等待、自然周执行、8/8.5%边界、保护覆盖普通计划、冻结恢复、长假、未知日历/缺日、开盘模拟、重复回放、报价失效、来源失败保留旧结果和前端新鲜度。
821次历史复盘与原独立基准/桌面纯内核一致，使用的是既有本地审计样本，不冒称821次实盘运行。
已显式重建一个旧审计价格差异和三个成交量取整例外，4296根历史指纹一致；后续新差异阻断。
本地真实页面检查：52/26周切换、Home/End、点选、390px窄屏无横向溢出、浏览器无error/warn；本地fixture明确标记不可交易。
